{
    "CN": "{{ inventory_hostname }}",
    "hosts": [
        "localhost",
        "127.0.0.1",
        "{{ ip | default(ansible_default_ipv4.address) }}",
        "{{ inventory_hostname }}"
    ],
    "key": {
        "algo": "rsa",
        "size": {{ certificates_key_size }}
    }
}