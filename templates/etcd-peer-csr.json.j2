{
    "CN": "{{ inventory_hostname }}",
    "hosts": [
        "localhost",
        "127.0.0.1",
        "{{ ip | default(ansible_host) }}",
        "{{ inventory_hostname }}"
    ],
    "key": {
        "algo": "rsa",
        "size": {{ certificates_key_size }}
    }
}